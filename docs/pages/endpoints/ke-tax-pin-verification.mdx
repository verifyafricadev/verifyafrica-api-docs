---
title: Kenya Tax PIN Verification
description: Verify Kenyan KRA Personal Identification Numbers.
sidebar_label: Kenya Tax PIN Verification
---

# Kenya Tax PIN Verification

Verify Kenyan KRA Personal Identification Numbers.

:::tip API Reference
Jump directly to the interactive API docs:

- [API reference](/api) — schemas, examples, and playground
- Create: `POST /api/v2/public/verifications/requests/`
- Detail: `GET /api/v2/public/verifications/requests/{verification_id}/detail/`
:::

## Verification Type

```
ke_tax_pin_verification
```

## Use Cases

- KRA PIN validation
- Taxpayer identity verification

## Input Parameters

Pass these fields inside `input_data` in your request body.

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `tax_pin` | string | Yes | Kenya Revenue Authority PIN (e.g., A123456789B). |
| `name` | string | No | Taxpayer name for cross-validation. |

## Making a Request

Public API creates always use **direct (offsite)** mode. Do not send `method_type` — the API forces `offsite`.


**Endpoint:** `POST https://api.verifyafrica.io/api/v2/public/verifications/requests/`

Authenticate with `Authorization: Bearer <VA-…api-key>` and set `verification_type` to `ke_tax_pin_verification`.

```json
{
  "verification_type": "ke_tax_pin_verification",
  "input_data": { /* see parameters above */ }
}
```



## Example Error Responses

Errors use the v2 envelope `{ "success": false, "message": "…", "errors": ["…"] }`.

```json
{
  "success": false,
  "message": "Invalid API key.",
  "errors": ["Invalid API key."]
}
```

```json
{
  "success": false,
  "message": "Missing required parameters for 'ke_tax_pin_verification': …",
  "errors": ["Missing required parameters for 'ke_tax_pin_verification': …"]
}
```

See the [Introduction](/) for the full set of status-code examples, or the interactive [API Reference](/api).

## API Reference

View the full request/response schemas, response codes, and interactive playground:

- [API reference](/api)
