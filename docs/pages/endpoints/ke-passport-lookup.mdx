---
title: Kenya Passport Lookup
description: Lookup Kenyan international passport records from immigration registries.
sidebar_label: Kenya Passport Lookup
---

# Kenya Passport Lookup

Lookup Kenyan international passport records from immigration registries.

:::tip API Reference
Jump directly to the interactive API docs:

- [API reference](/api/single-gov-kenya#kenya-passport-lookup) — schemas, examples, and playground
- Create: `POST /api/v2/public/verifications/requests/government_registry_checks/`
- Detail: `GET /api/v2/public/verifications/requests/?verification_id={verification_id}`
:::

## Verification Type

```
ke_passport_lookup
```

## Use Cases

- Passport record verification
- Immigration identity checks

## Input Parameters

Pass these fields inside `input_data` in your request body.

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `passport_number` | string | Yes | Kenya passport number (e.g., A1234567). |
| `first_name` | string | No | First name for cross-validation. |

## Making a Request

Public API creates always use **direct (offsite)** mode. Do not send `method_type` — the API forces `offsite`.


**Endpoint:** `POST https://api.verifyafrica.io/api/v2/public/verifications/requests/government_registry_checks/`

Authenticate with `Authorization: Bearer <VA-…api-key>` and set `verification_type` to `ke_passport_lookup`.

```json
{
  "verification_type": "ke_passport_lookup",
  "input_data": { /* see parameters above */ }
}
```



## Example Error Responses

Errors use the v2 envelope `{ "success": false, "message": "…", "errors": ["…"] }`.

```json
{
  "success": false,
  "message": "Invalid API key.",
  "errors": ["Invalid API key."]
}
```

```json
{
  "success": false,
  "message": "Missing required parameters for 'ke_passport_lookup': …",
  "errors": ["Missing required parameters for 'ke_passport_lookup': …"]
}
```

See the [Introduction](/) for the full set of status-code examples, or the interactive [API Reference](/api).

## API Reference

View the full request/response schemas, response codes, and interactive playground:

- [API reference](/api/single-gov-kenya#kenya-passport-lookup)