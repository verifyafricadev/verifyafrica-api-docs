---
title: Nigeria Phone Lookup
description: Lookup subscriber information associated with a Nigerian phone number.
sidebar_label: Nigeria Phone Lookup
---

# Nigeria Phone Lookup

Lookup subscriber information associated with a Nigerian phone number.

:::tip API Reference
Jump directly to the interactive API docs:

- [API reference](/api) — schemas, examples, and playground
- Create: `POST /api/v2/public/verifications/requests/`
- Detail: `GET /api/v2/public/verifications/requests/{verification_id}/detail/`
:::

## Verification Type

```
ng_phone_number_lookup
```

## Use Cases

- Reverse phone lookup
- Subscriber identity discovery

## Input Parameters

Pass these fields inside `input_data` in your request body.

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `phone_number` | string | Yes | Nigerian phone number to look up. |

## Making a Request

Public API creates always use **direct (offsite)** mode. Do not send `method_type` — the API forces `offsite`.


**Endpoint:** `POST https://api.verifyafrica.io/api/v2/public/verifications/requests/`

Authenticate with `Authorization: Bearer <VA-…api-key>` and set `verification_type` to `ng_phone_number_lookup`.

```json
{
  "verification_type": "ng_phone_number_lookup",
  "input_data": { /* see parameters above */ }
}
```


## API Reference

View the full request/response schemas, response codes, and interactive playground:

- [API reference](/api)
