---
title: Nigeria Passport Verification
description: Verify Nigerian international passport numbers against NIS records.
sidebar_label: Nigeria Passport Verification
---

# Nigeria Passport Verification

Verify Nigerian international passport numbers against NIS records.

:::tip API Reference
Jump directly to the interactive API docs:

- [API reference](/api) — schemas, examples, and playground
- Create: `POST /api/v2/public/verifications/requests/`
- Detail: `GET /api/v2/public/verifications/requests/{verification_id}/detail/`
:::

## Verification Type

```
ng_passport_verification
```

## Use Cases

- Passport authenticity checks
- Travel and immigration KYC

## Input Parameters

Pass these fields inside `input_data` in your request body.

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `passport_number` | string | Yes | Passport number (e.g., A12345678). |
| `first_name` | string | No | First name for cross-validation. |
| `last_name` | string | No | Last name for cross-validation. |
| `date_of_birth` | string | No | Date of birth (YYYY-MM-DD). |

## Making a Request

Public API creates always use **direct (offsite)** mode. Do not send `method_type` — the API forces `offsite`.


**Endpoint:** `POST https://api.verifyafrica.io/api/v2/public/verifications/requests/`

Authenticate with `Authorization: Bearer <VA-…api-key>` and set `verification_type` to `ng_passport_verification`.

```json
{
  "verification_type": "ng_passport_verification",
  "input_data": { /* see parameters above */ }
}
```



## Example Error Responses

Errors use the v2 envelope `{ "success": false, "message": "…", "errors": ["…"] }`.

```json
{
  "success": false,
  "message": "Invalid API key.",
  "errors": ["Invalid API key."]
}
```

```json
{
  "success": false,
  "message": "Missing required parameters for 'ng_passport_verification': …",
  "errors": ["Missing required parameters for 'ng_passport_verification': …"]
}
```

See the [Introduction](/) for the full set of status-code examples, or the interactive [API Reference](/api).

## API Reference

View the full request/response schemas, response codes, and interactive playground:

- [API reference](/api)
